# SnapChef Backend (FastAPI)

Minimal FastAPI backend for SnapChef.

## Quick start
```bash
python -m venv .venv
# Windows PowerShell:
. .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app.main:app --reload
